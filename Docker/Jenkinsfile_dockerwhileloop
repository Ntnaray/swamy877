pipeline {
 agent {docker 
 {image 'ubuntu:latest
 '}
 }
stage {
    stage ('Build') {
        steps {
            sh "echo ${env.workspace}"
            sh "ls -ltr"
            sh "Docker/whileloop.sh"
        }
    }
}
}